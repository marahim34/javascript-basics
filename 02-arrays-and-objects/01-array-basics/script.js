// Array Literal
const numbers = [1, 2, 3, 4, 5, 6, 7];
const mixed = [12, 'hello', true, null]

// Array constructor
const fruits = new Array('Apple', 'Banana', 'Cherry', 'Date', 'Eggplant', 'Fig');

x = numbers[0];

x = numbers[0] + numbers[3];

x = `My favorite fruit is ${fruits[3]}`;

x = numbers.length;

// fruits.length = 4;

fruits[1] = 'Berry';

fruits[fruits.length] = 'Guava';

x = fruits;

console.log(x);